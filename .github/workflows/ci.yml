name: CI
on:
  pull_request:
  push:
    branches:
      - main
jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3.0.2
      - name: Run
        uses: ./
        with:
          repo-token: ${{ secrets.GITHUB_TOKEN }}